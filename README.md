# Azure Terraform Resource Stacks

These Terraform provisionable resource stacks are provided for convenience and as a starting point to build your own.

This repository provides a minimalistic, but extensible provisioning sandbox. For the sake of simplicity, this sandbox assumes the authentication towards Azure is done using a Service Principal and password based credentials.

